@model IEnumerable<ProiectDAWcod.Data.goal>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.tip_corp)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.descriere)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.tip_corp)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.descriere)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id_goal }) |
            @Html.ActionLink("Details", "Details", new { id=item.id_goal }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id_goal })
        </td>
    </tr>
}

</table>
